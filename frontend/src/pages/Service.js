import React from "react";
import ServicesContainer from "../containers/ServiceContainer";

const Services = ({ match }) => {
    return (
        <ServicesContainer />
    );
};
export default Services;
