import React from "react";
import UserRulesContainer from "../containers/UserRulesContainer";

const UserRules = ({ match }) => {
    return (
        <UserRulesContainer />
    );
};
export default UserRules;
